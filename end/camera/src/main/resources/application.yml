spring:
  application:
    name: camera
server:
  port: 8092

eureka:
  client:
    registry-fetch-interval-seconds: 20
    service-url:
      defaultZone: http://localhost:7777/eureka

camera:
  step: 45